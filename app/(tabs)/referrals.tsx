import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import Referrals from "@/components/Main/Referrals";

const Services = () => {
  return (
    <SafeAreaView>
      <Referrals />
    </SafeAreaView>
  );
};

export default Services;
